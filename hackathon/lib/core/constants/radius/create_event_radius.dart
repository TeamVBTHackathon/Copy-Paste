import 'package:flutter/material.dart';

class CreateEventRadius {
  static final BorderRadius createEventSelectCity = BorderRadius.circular(10);
  static final BorderRadius createEventSelectCategory =
      BorderRadius.circular(10);
  static final BorderRadius submitButtonRadius = BorderRadius.circular(30.0);
}
