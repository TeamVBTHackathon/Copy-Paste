import 'package:flutter/widgets.dart';

class HomePageRadius {
  static final BorderRadius homeSearchBarRadius = BorderRadius.circular(20);
  static const double circleAvatarRadius = 24;

  static const BorderRadius homeBottomContainer = BorderRadius.only(
    topLeft: Radius.circular(30),
    topRight: Radius.circular(30),
  );
}
