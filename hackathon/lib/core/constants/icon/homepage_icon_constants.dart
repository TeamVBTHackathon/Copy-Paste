import 'package:flutter/material.dart';

import '../../init/theme/theme_purple.dart';

class HomePageIcon {
  static const IconData locationIcon = Icons.location_on;
  static const Icon searchIcon = Icon(Icons.search);
  static const Icon homePaletIcon = Icon(
    Icons.palette,
    color: ThemePurple.darkPurple,
    size: 30,
  );

  static const Icon homeBottomLocationIcon = Icon(Icons.location_on_rounded);
}
