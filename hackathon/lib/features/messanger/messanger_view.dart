import 'package:flutter/material.dart';

class MessangerView extends StatelessWidget {
  const MessangerView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('MessangerView'),
    );
  }
}
